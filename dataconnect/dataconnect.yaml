specVersion: "v1beta"
serviceId: "mywebapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mywebapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
